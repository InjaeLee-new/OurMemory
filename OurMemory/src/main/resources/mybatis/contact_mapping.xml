<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.contactMapper">
   <!--  //insert : 글저장 -->
	<insert id="contactInsert" parameterType="contactDTO">
		insert into contact values
		(#{contact_name},#{contact_id}, #{contact_pwd}, #{contact_email1}, #{contact_email2}, #{contact_tel1}, #{contact_tel2}, #{contact_tel3}, #{contact_reason}, #{contact_callback},#{contact_subject},#{contact_content}, sysdate)
	</insert>

   <!-- // select : 본인이 쓴 글 불러오기 (list)  -->
	<select id="contactSelectAll" parameterType="String" resultType="contactDTO">
		select * from contact where contact_id =#{contact_id}
	</select>
	
	<!-- // select : 해당 글 불러오기 (선택한 1개만) -->
	<select id="contactSelectOne" parameterType="java.util.Map" resultType="contactDTO">
		select * from contact where contact_id =#{contact_id} and contact_subject =#{contact_subject}
	</select>
	
	<!-- // update : 내용 수정하기 -->
	<update id="contactUpdate" parameterType="java.util.Map">
	update contact set contact_email1=#{contact_email1}, contact_email2=#{contact_email2}, contact_tel1=#{contact_tel1} , 
	contact_tel2=#{contact_tel2}, contact_tel3=#{contact_tel3}, contact_reason=#{contact_reason}, 
	contact_callback= #{contact_callback}, contact_subject=#{contact_subject},contact_content=#{contact_content}
    where contact_id=#{contact_id} and contact_subject =#{contact_subject}
	</update>
	
	<!-- // delete : 글 삭제하기 -->
	<delete id="contactDelete" parameterType="java.util.Map">
	delete contact where contact_id=#{contact_id} and contact_subject =#{contact_subject}
	</delete>
</mapper>